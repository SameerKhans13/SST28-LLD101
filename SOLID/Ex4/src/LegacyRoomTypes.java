// legacy integer room types kept for backward compatibility, but
// new code should use the `RoomType` enum instead.
@Deprecated
public class LegacyRoomTypes {
    // kept only for historical reasons; do not use in new code
}
